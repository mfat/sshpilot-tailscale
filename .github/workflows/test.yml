name: test

on: [push, pull_request]

jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # sshpilot.plugins.api has no GTK dependency, so --no-deps keeps CI light.
      - run: pip install pytest "sshpilot @ git+https://github.com/mfat/sshpilot" --no-deps
      - run: pytest -ra
